<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BleDfuTesterActivity" parent="android:Theme.Material.Light.NoActionBar" />
    <!-- Base application theme. -->
    <style name="Base.Theme.BleDfuTesterActivity" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Compact outlined button: used for inline secondary actions (Rescan / Change) -->
    <style name="Widget.App.Button.Compact.Outlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:minHeight">28dp</item>
        <item name="android:paddingTop">0dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!-- Compact text button: used for low-emphasis actions (New Test, Share Log) -->
    <style name="Widget.App.Button.Compact.Text" parent="Widget.Material3.Button.TextButton">
        <item name="android:minHeight">28dp</item>
        <item name="android:paddingTop">0dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!-- Larger, prominent button for the primary "pick firmware" action -->
    <style name="Widget.App.Button.SelectFile" parent="Widget.Material3.Button.TonalButton">
        <item name="android:minHeight">56dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>